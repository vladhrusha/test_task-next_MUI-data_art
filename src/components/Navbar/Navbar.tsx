import React from 'react';

const Navbar = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '100px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        color: 'white',
        padding: '40px',
        fontSize: '32px',
      }}
    >
      Jokeree
    </div>
  );
};

export default Navbar;
